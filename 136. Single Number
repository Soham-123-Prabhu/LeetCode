class Solution(object):
    def singleNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        a=0
        for x in nums:
            a^=x.  #we use XOR if we want to find duplicates using no extra space. REMEMBER THIS STEP
        return a
        
